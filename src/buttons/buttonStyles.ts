import type { ButtonSize } from "./Button";

/** Shared icon/chevron size classes for button variants. */
export const ICON_SIZE_CLASS: Record<ButtonSize, string> = {
  sm: "size-3",
  md: "size-4",
  lg: "size-5",
};
